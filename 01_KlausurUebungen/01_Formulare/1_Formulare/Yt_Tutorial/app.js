var playerName = 'Max'; // deklariert und initialisiert
var highscore = 100; // Datentyp number
var gameOver = false; 
var test; // Datentyp undefined 
var nüscht = null; // Datentyp object

let secondPlayer = 'Jan';

if(true){
    var playerName = "Mario"
}

console.log(typeof gameOver); // sagt was für ein Datentyp das ist  

console.log(playerName);

// Funktionen 

function calculate(number1, number2){
    console.log(number1 + number2);
}

calculate(); // Jetzt wird es ausgegeben